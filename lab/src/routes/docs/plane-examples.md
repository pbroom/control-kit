# Plane Examples

Focused examples of the same `Plane` primitive adapted to different two-dimensional value semantics. Every example uses normalized X and Y coordinates, then maps them into its own labels, visual feedback, constraints, and accessible value text.

<!-- demo:gallery -->
